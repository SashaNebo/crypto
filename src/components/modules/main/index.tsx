import { Hero } from "@/components/sections/hero"
import { Stripe } from "@/components/elements/stripe"

export const Main = () => {
  return (
    <main>
      <Hero />
      {/* <Stripe /> */}
    </main>
  )
}
